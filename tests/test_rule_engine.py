"""STUB — Phase 1. rule_engine.py has no task-specific logic to test yet
(see agent/rule_engine.py's module docstring). Real tests replace this
file once the task is uploaded and rule_engine.decide() is implemented.
"""
import pytest


@pytest.mark.skip(reason="rule_engine.py is a Phase 1 stub — see Section 12 of the master build prompt")
def test_rule_engine_placeholder() -> None:
    pass
