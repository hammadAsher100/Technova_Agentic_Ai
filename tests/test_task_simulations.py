"""STUB — Phase 1. Full simulated match runs against simulations.SCENARIOS
(see simulations/scenarios.py's module docstring). Real tests replace
this file once the task is uploaded and its genre is known (Section 11).
"""
import pytest


@pytest.mark.skip(reason="simulations/scenarios.py is a Phase 1 stub — see Section 11 of the master build prompt")
def test_task_simulations_placeholder() -> None:
    pass
